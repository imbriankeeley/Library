const myLibrary = [];

function Book(title, author, pages, read) {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.read = read;
    this.addBookToLibrary();
}

function addBookToLibrary() {
    myLibrary.push
}

myLibrary.forEach(element => {
    
});

const harryPotter = new Book('Harry Potter', 'Jk Rowling', '700 pages', 'have not read')